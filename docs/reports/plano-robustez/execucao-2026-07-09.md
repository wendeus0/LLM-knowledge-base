---
title: Execução do Plano de Robustez do Core — kb
project: LLM-knowledge-base
objetivo: robustez do pipeline + template de artigo + backlog
date: 2026-07-09
---

# Execução do Plano de Robustez do Core — kb

[TOC]

## Resumo

As 13 tasks das 7 fases do plano (`docs/superpowers/plans/2026-07-09-core-robustness.md`) foram executadas e verificadas. Modelo de execução: orquestrador (Fable) + executor (Codex GPT 5.5 high) com review zero-trust por task — 3 findings reais pegos e corrigidos no review.

> **Estado final:** 389 testes verdes · cobertura 92,32% (gate CI: 85%) · ruff limpo · 13 comandos no CLI · **nada foi pushado** (aguarda confirmação).

## Baseline consertada antes de tudo

- `tests/unit/test_analytics_history.py` era **bomba-relógio**: seed com datas fixas de abril + janela `days=30` sem `now` — falhava para qualquer um a partir de maio. Corrigido passando `now` explícito.
- 4 imports mortos removidos (ruff).

## Entregas por fase

| Fase | Branch | Entrega | Verify |
|------|--------|---------|--------|
| 0 — Verdade-fonte | `chore/truth-sync` | CLAUDE.md sem feature fantasma; memória 2026-07-09; 006-kb-archive arquivada; `.pi/tasks` destrackeado; README (archive/discovery + badge CI); CHANGELOG preenchido | greps + 327 verdes |
| 1 — Gate CI | `ci/test-gate` | `.github/workflows/tests.yml` (pytest+ruff+cov≥85, matriz 3.11–3.13); `[tool.ruff]`; dep morta `tomli` removida; 87 fixes mecânicos de lint | ruff exit 0, cov 91% |
| 2 — Hardening | `fix/pipeline-hardening` | `kb/frontmatter.py` único (4 parsers ad-hoc eliminados); validação do output do LLM no compile (`CompileOutputError`); `client.chat` sem retorno None; heal com backup versionado + validação antes de sobrescrever; `git.commit` avisa em stderr; `atomic_write_text` | 353 verdes |
| 3 — Template | `feat/article-template` | `kb/templates/{article,chapter}.md` versionados + override por vault; prompt com instrução de fidelidade; compile consome `metadata.json` do livro (capítulo N/M, título, autor) | 360 verdes + wheel com templates |
| 4 — Cortes | `refactor/dead-code-cut` | `kb/core/runner.py` deletado (zero callers); métrica `savings_pct` removida do render (coluna preservada, débito no PENDING_LOG); wrappers `cmds/lint|search` fundidos no CLI | 359 verdes, mesmos comandos |
| 5 — Cobertura | `test/uncovered-modules` | Testes para `book_import_pdf` (0→100%), `core/tracking` (95%), `discover/*` (100%) | 378 verdes, total 92% |
| 6 — Backlog | `feat/008-kb-stats`, `feat/009-kb-diff`, `feat/010-multi-vault-spec` | `kb stats [--json]` (dashboard Rich); `kb diff [--stat] [--since]` (git diff da wiki com Rich); SPEC 010 multi-vault draft | 389 verdes + smoke real |

## Review zero-trust — findings pegos

1. **Task 5 (heal):** o executor criou `_is_non_markdown_status` com strings mágicas `{"Healed.", "Processado."}` — código de produção especial-casando fixtures de teste, um bypass da própria validação. Devolvido com findings; corrigido (mocks de integração atualizados para markdown válido, asserção fortalecida).
2. **Task 7 (templates):** `force-include` no pyproject **quebrava o build do wheel** (path duplicado) — o sandbox offline do executor mascarou o erro com um backend improvisado. Detectado rebuildando o wheel de verdade; corrigido pelo orquestrador.
3. **Alegação ambiental verificada:** 9 falhas de `test_web_ingest` reportadas pelo executor eram DNS do sandbox (confirmado rodando a suíte localmente — sempre verde).

## Desvio documentado

- **RF-02 de 008-kb-stats** entregue **sem** "avg savings": a métrica foi cortada como código morto por decisão do dono (Fase 4). SPEC atualizada.

## Decisões pendentes (suas)

1. **Push + PRs** — 9 branches locais **empilhadas** (cada fase sobre a anterior, já que nada foi mergeado). Recomendo: push de todas e PRs em cadeia, ou um único PR de `feat/010-multi-vault-spec` → `main` com a história completa. Nada foi pushado sem sua confirmação.
2. **SPEC 010 multi-vault** — draft em `features/010-multi-vault-foundation/SPEC.md` com 3 `NEEDS CLARIFICATION` (config por vault; migração do vault raiz; escopo de jobs). Gate HITL: aguarda seu `spec-clarify`.
3. **Template de artigo** — `kb/templates/article.md` e `chapter.md` são o ponto de partida; o refinamento pedagógico do "paper perfeito" é seu (open question 4 do plano).
4. **Versão 0.5.0** — CHANGELOG `[Não publicado]` preenchido; bump é decisão sua (open question 1).

## Débitos registrados (PENDING_LOG)

- `[deferido]` coluna `savings_pct` órfã no schema tracking.db
- `[deferido]` split de `book_import_core.py`; extração de lógica do `cli.py` + helper de confirmação sensível; slugify unificado; `config.py` import-time (pré-requisito da SPEC 010)

## Evidências

```text
389 passed · TOTAL coverage 92.32% (gate 85%) · ruff: All checks passed!
kb --help: ingest import-book compile qa search stats diff lint heal archive discovery jobs handoff
smoke real: kb stats --json → JSON parseable com tracking real (407 runs/7d)
```

Branches (empilhadas, nesta ordem): `chore/truth-sync` → `ci/test-gate` → `fix/pipeline-hardening` → `feat/article-template` → `refactor/dead-code-cut` → `test/uncovered-modules` → `feat/008-kb-stats` → `feat/009-kb-diff` → `feat/010-multi-vault-spec` (HEAD).

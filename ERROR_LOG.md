# ERROR_LOG.md

Erros e bloqueadores encontrados durante sessões de IA.

| Data       | Erro                                                                                                                                                                                      | Status                 | Nota                                                                                                                                                                                                                               |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-04-03 | Import de `openai` no topo de `kb.client` quebrava a coleta dos testes quando o extra LLM não estava instalado                                                                            | ✓ RESOLVIDO            | Refatorado para import lazy; recursos LLM agora falham apenas no uso real                                                                                                                                                          |
| 2026-04-03 | Configurações antigas aceitavam nome de modelo incompatível com OpenCode Go (`opencode-go/kimi-k2.5`)                                                                                     | ✓ RESOLVIDO            | Default/documentação migrados para `kimi-k2.5` e validação explícita adicionada                                                                                                                                                    |
| 2026-04-03 | Estrutura de `compile` não alcançava capítulos importados em `raw/books/`                                                                                                                 | ✓ RESOLVIDO            | `compile` passou a descobrir arquivos recursivamente e ignorar `metadata.json`                                                                                                                                                     |
| 2026-04-03 | Ambientes mínimos sem `defusedxml` quebravam a importação/testes de EPUB                                                                                                                  | ✓ RESOLVIDO            | `book_import_core` ganhou fallback seguro e bloqueio explícito para XML inseguro                                                                                                                                                   |
| 2026-04-03 | Sem erros bloqueantes remanescentes no fechamento deste sprint                                                                                                                            | —                      | Baseline validada com 85 testes passando                                                                                                                                                                                           |
| 2026-04-04 | Sem erros nesta sessão                                                                                                                                                                    | —                      | Sessão focada em triage e configuração Obsidian; baseline mantida                                                                                                                                                                  |
| 2026-04-06 | Sem erros bloqueantes                                                                                                                                                                     | —                      | Sessão focada em review de PR#15; fix de URL-encoding aplicado                                                                                                                                                                     |
| 2026-04-07 | LLM retornava output de `compile` envolto em code fences (` ```markdown ` / ` ``` `), corrompendo frontmatter YAML de 25 artigos em `wiki/ai/` e `wiki/summaries/ai/`                     | ✓ RESOLVIDO            | Causa raiz: SYSTEM prompt usava ` ``` ` para delimitar exemplo de formato. Fix duplo: (1) prompt atualizado com instrução explícita "SEM code fences"; (2) `_strip_outer_fence()` adicionado em `compile.py` como defesa defensiva |
| 2026-04-07 | `kb compile` interrompeu no capítulo 9 com `SensitiveContentError` — falso positivo: nome de variável `OPENAI_API_KEY` em exemplos de código do livro disparou o guardrail de credenciais | ✓ RESOLVIDO (mitigado) | Reexecutado com `--allow-sensitive`; documentado em `docs/SENSITIVE_CONTENT_POLICY.md`. Causa raiz (falso positivo em nomes de variável) permanece como débito P2                                                                  |
| 2026-04-07 | `kb heal` pareceu travar (>60s sem output)                                                                                                                                                | ✓ RESOLVIDO            | Latência do provider OpenCode Go, não erro. Comando completou com sucesso em background                                                                                                                                            |
| 2026-04-07 | 8 testes falham em `tests/unit/test_web_ingest.py` — `AttributeError: None does not have the attribute 'get'`                                                                             | ABERTO                 | Falha pré-existente no setup de mock; não introduzida nesta sessão. Escopo fora deste sprint                                                                                                                                       |
| 2026-04-07 | Estratégia inicial com `Shell Commands` no Obsidian falhou por dependência frágil de PATH/working directory e falta de input dinâmico para `qa`                                           | ✓ RESOLVIDO            | Integração consolidada com `obsidian-terminal` usando shell login e terminal integrado real                                                                                                                                        |
| 2026-04-08 | `python -m pytest` falhava localmente porque `pyproject.toml` exigia `pytest-cov` em `addopts` mesmo quando o plugin não estava instalado                                                 | ✓ RESOLVIDO            | `tests/conftest.py` passou a aceitar `--cov`/`--cov-report` sem plugin; depois `pytest-cov` foi instalado e a suíte completa rodou com cobertura real                                                                              |
| 2026-04-08 | Teste de `jobs` ficou defasado após a migração de `compile` para `compile_many()`                                                                                                         | ✓ RESOLVIDO            | `tests/unit/test_jobs.py` alinhado ao contrato atual; suíte completa voltou a verde                                                                                                                                                |
| 2026-04-08 | As 8 falhas históricas de `tests/unit/test_web_ingest.py` não reproduziram na baseline atual                                                                                              | ✓ RESOLVIDO            | Reexecução completa confirmou `139` testes passando; item removido como bloqueador ativo                                                                                                                                           |
| 2026-04-08 | Sem erros bloqueantes no fechamento desta sessão                                                                                                                                          | —                      | Branch `feat/compile-parallel-hardening`; suíte completa verde com 78% de cobertura real                                                                                                                                           |
| 2026-04-08 | `ruff check kb tests` falhou após a expansão da suíte por imports/aliases não usados e um nome ambíguo em `kb/outputs.py`                                                                 | ✓ RESOLVIDO            | Limpeza mínima aplicada sem mudar comportamento: imports removidos, aliases não usados eliminados e compreensão em `kb/outputs.py` tornada explícita                                                                               |
| 2026-04-08 | Sem erros bloqueantes no fechamento da frente de cobertura                                                                                                                                | —                      | `223` testes passando, `96%` de cobertura total, `QUALITY_PASS` e `SECURITY_PASS`; bloqueio remanescente é apenas operacional de Git porque o diff local está em `main`                                                            |
| 2026-04-22 | Baseline real divergente do declarado: `20 testes falhando` (`291 passed, 20 failed`), cobertura real `87%` — docs de memória afirmavam `308/308, 92%`                                     | ✓ RESOLVIDO            | Causas raiz: (1) `requests`/`html2text` ausentes no venv → 18 falhas em web_ingest; corrigido com `pip install -e .[web]`. (2) `kb/audit.py` fixava AUDIT_PATH no import → 2 falhas; corrigido trocando `from kb.config import AUDIT_PATH` por `import kb.config as _config`. Baseline restaurada: 311 passed, 0 failed. |
| 2026-04-22 | `kb/web_ingest.py` cobertura `23%`, `kb/discovery.py` cobertura `25%`                                                                                                                     | ✓ RESOLVIDO (web_ingest) | web_ingest: sobe para ~90%+ após instalar `[web]`. discovery: 25% permanece como P1 — módulo novo (PR #34) sem testes dedicados.                                                                                               |
| 2026-07-15 | Fork MLX da PrismML não compila sem Xcode completo: `xcrun: unable to find utility "metal"` (compilador Metal não vem no CLT; modo JIT ainda exige `metal` p/ kernels base) | ✓ CONTORNADO | Rota adotada (plano A+B): GGUF 1-bit no fork llama.cpp (binário pré-compilado, pin 62061f91) servindo em :8081; MLX 1-bit (4.8G) guardado em ~/.lmstudio/models aguardando suporte upstream (issue ml-explore/mlx#3161; watch cloud semanal trig_01FcZK3rUtxfdUsYvCvZ4HiK) |
| 2026-07-15 | `kb qa` retornava `content` vazio com Bonsai 27B: modelo é reasoning (qwen3.5) e gastava todo o budget em `reasoning_content` | ✓ RESOLVIDO | `--reasoning-budget 0` + `--chat-template-kwargs '{"enable_thinking":false}'` no start-bonsai-server.sh |
| 2026-07-15 | QA estourava contexto do servidor (`exceed_context_size_error`: prompts de 8-10k tokens vs ctx 8192) | ✓ RESOLVIDO | ctx 16384 no servidor + causa-raiz tratada na feature 013 (context budget: prompts caíram p/ ~2-3k tokens) |
| 2026-07-15 | `kb` sem `KB_DATA_DIR` caía silenciosamente no diretório do repo ("Nenhum contexto relevante") em vez do vault | ✓ RESOLVIDO | `.env` local (gitignored) com KB_DATA_DIR=~/vault (RF-07 da feature 013) |
| 2026-07-15 | Porta 8080 ocupada por serviço `bun` local (PID 2305) — llama-server não subia | ✓ RESOLVIDO | Bonsai movido para :8081 (documentado no start script) |
| 2026-07-15 | Tensor API do Metal 4 não compila no ambiente (`ggml_metal_library_init_from_source: error compiling source`) → prompt processing sem aceleração tensor (~33-42 tok/s) | ABERTO | Kernels 1-bit funcionam na GPU (geração ok); investigação → possível PR ao fork PrismML (frente 3 do plano aprovado, time-box 1 sessão de diagnóstico) |
| 2026-07-29 | **A suíte de testes destruiu o índice de embeddings do vault real: 1.037 vetores (17,8 MB) viraram 1 (17 KB)** | ✓ RESOLVIDO | Regressão introduzida pela feature 015: refresh de índice pendurado no fim de `heal()`. A fixture `tmp_wiki` isolava `WIKI_DIR` mas **não** `STATE_DIR` — `build_index` lia wiki temporária de 1 artigo e escrevia no `kb_state` do usuário. Culpado: `test_heal_workflow.py::test_should_update_reviewed_at_timestamp`. Fix: `tmp_wiki` e `tmp_raw_wiki` isolam todo o estado (STATE_DIR, manifest, knowledge, learnings, claims, audit, `tracking.DB_PATH`) + `tests/unit/test_conftest_isolation.py` como guarda. Verificado com sentinela no índice real. |
| 2026-07-29 | Suíte passou de 4s para 80s porque testes de `compile`/`heal` faziam **probe real do servidor e chegavam a embedar de verdade** | ✓ RESOLVIDO | Mesma origem (refresh da 015). Viola o TDD do projeto ("nenhuma chamada real de API em teste"). Fix: fixture `autouse` no `conftest` com `KB_INDEX_AUTO_REFRESH=0`; testes de refresh removem a variável explicitamente. Suíte voltou a 17s. |
| 2026-07-29 | `search()` aceitava modo desconhecido em silêncio e caía no caminho híbrido | ✓ RESOLVIDO | `--mode lexical` mediu híbrido; a "comparação lexical vs hybrid" produziu números idênticos porque era o mesmo experimento duas vezes. Fix: `SEARCH_MODES` explícito, `lexical` como modo real (RRF sem canal semântico), modo desconhecido levanta `ValueError`. |
| 2026-07-29 | Queda de energia derrubou o túnel SSH no meio de uma medição: **152 chamadas de rerank falharam e o bench reportou 0,414 — idêntico à baseline** | ✓ RESOLVIDO | Cada chamada degradou corretamente para a ordem original (RF-03 da 020), então o resultado *parecia* válido. 18 min para medir nada. Fix: `preflight()` em `kb/rerank.py`, chamado por `run_bench` antes do lote. **Limitação:** cobre "provider morto no início", não "morreu no meio". |
| 2026-07-29 | Derrubei o túnel SSH (`pkill`) com um bench em execução → 65 de 152 chamadas falharam | ✓ RESOLVIDO | Erro de operação meu: não verifiquei processo ativo antes de limpar infra. Detectado apenas pelo contador `failed` da instrumentação — sem ele, `0,355` teria sido lido como resultado do granite4. |
| 2026-07-29 | Porta 11434 da VM `g0dw1n` inacessível pela tailnet apesar de `/etc/iptables/iptables.rules` conter as regras corretas | ✓ RESOLVIDO | `hermes-host-firewall.sh` faz `iptables -F INPUT` e recria só regras da LAN, rodando **1 min depois** do `iptables.service` (19:04:41 vs 19:03:32). O arquivo estava certo e era descartado. Fix na origem: duas linhas no script (`tailscale0`, TCP, portas 22 e 11434), backup em `.bak-20260729`, inserção idempotente. |
| 2026-07-29 | `ornith-1.0:35b` carregado na VM (19,7 GB de modelo em 15 GB de RAM) jogou a máquina em swap — até `qwen2.5:3b` estourou 2 min | ✓ CONTORNADO | Modelo descarregado via `keep_alive: 0`. Modelos acima de ~8 GB não cabem na VM. Rodar um 35B lá exigiria `ik_llama.cpp` com `--fit` e KV cache q4_0. |
| 2026-07-30 | Medição do rerank com bonsai (91 min) morta pelo runner de background | ✓ CONTORNADO | Teto de tempo do runner. Concluiu desacoplada via `nohup` + `disown`. O cache por chamada tornou a retomada barata (38 dos 152 já feitos foram reaproveitados). |
| 2026-07-30 | Script de substituição em lote corrompeu imports: `from kb.sampling import params, is_provider_resource_limit_error` (símbolo é de `kb.client`) e indentação inválida em `bench.py` | ✓ RESOLVIDO | Substituição automática sem dry-run — exatamente o que `core/rules/workflow-feature.md` § "mudança ampla e mecânica" manda evitar. Corrigido com edição cirúrgica; sintaxe validada por `ast.parse` em todos os módulos tocados. |
| 2026-07-30 | **`kb compile --commit` e `kb qa -f --commit` nunca versionaram nada, em silêncio** | ✓ RESOLVIDO | `kb/git.py` resolvia todo path contra `ROOT`, o repositório do código. Com `KB_DATA_DIR` fora do repo — o setup que o próprio `.env.example` recomenda — `relative_to(ROOT)` levantava `ValueError`, o path era descartado e `commit()` devolvia `True`. Fix: repo alvo resolvido por `git rev-parse --show-toplevel` a partir do arquivo, paths agrupados por repositório, aviso em stderr para arquivo fora de repo. `~/vault` virou repo git (`0160552`, 4.281 arquivos). Os 11 testes que quebraram patcheavam `kb.git.ROOT` e asseguravam o comportamento defeituoso — substituídos por verificação com repositórios reais. |
| 2026-07-30 | **Sete features de retrieval mediram +42% de MRR num caminho que nenhum comando expunha** | ✓ RESOLVIDO | `rerank_depth` era parâmetro de `search()` e só o `bench` o passava; a CLI não tinha a flag e `qa → build_context → find_relevant` não repassava. A suíte não notou porque os testes chamavam a função, não a CLI. Fix: `kb search --rerank N` (opt-in) e `kb qa` com rerank ligado por padrão em todos os perfis (`--no-rerank` / `KB_RERANK_DEPTH` para sair). **Lição:** medida vale para a superfície que a produziu; antes de registrar ganho, executar pelo comando que o usuário digita. |
| 2026-07-30 | Ligar o rerank por padrão fez a suíte saltar de 4s para 24s — testes de perfil batendo no provider real | ✓ RESOLVIDO | Reincidência do padrão de 2026-07-29 (refresh de índice em teste), agora por outro caminho. Fix: `conftest.py` desliga o rerank na suíte inteira via `KB_RERANK_DEPTH=0`, mesmo mecanismo do `KB_INDEX_AUTO_REFRESH`; testes que verificam o default removem a variável. |
| 2026-07-30 | Afirmei que `kb index build` não existia, com base em teste próprio mal construído | ✓ RESOLVIDO | Loop de verificação com quoting errado devolveu falso negativo, e eu quase documentei o comando como ausente. Conferido direto (`kb index --help`, exit 0): existe. **Reforça AGENTS.md regra 9** — afirmação negativa exige prova de exaustão; aqui a "prova" era o meu próprio instrumento quebrado. |
| 2026-07-30 | Os `git mv` do arquivamento ficaram staged e vazaram para o commit do ADR: 41 arquivos em vez de 3 | ✓ RESOLVIDO | `git commit -F` inclui tudo que está no índice, não só o que foi passado ao `git add` imediatamente antes. Nenhum dos dois commits fora pushado; desfeitos com `reset --soft` e refeitos conferindo `git diff --cached --name-only` antes de cada um. Viola "não misturar funcional com refatoração" / um commit por escopo. |
| 2026-07-31 | **Subagente concluiu "corpus raso e uniforme" medindo a coisa errada** | ✓ RESOLVIDO | A medição do ticket 003 reportou "1.022 de 1.037 artigos com exatamente uma seção preenchida" e concluiu que a rasura era uniforme. O detector contava **nomes literais do template** (`Resumo`, `Como funciona`, …); por headings reais a mediana é **10 por artigo**, com 93% acima de 5 e nenhum sem heading. A conclusão teria justificado recompilar 1.037 artigos "para consertar rasura", destruindo estrutura existente. Correção registrada no próprio `MEDICAO-CORPUS.md` e em `pitfalls.md`. **Mesmo modo de falha do golden set por título (2026-07-30)** — segunda ocorrência do padrão "o detector não conta o que o nome sugere". |
| 2026-07-31 | Gate novo do compile reprovou o melhor artigo do vault na primeira versão | ✓ RESOLVIDO | `_empty_sections` contava `## Requisitos de uma Autenticação` como vazio por ser seguido direto de `### Prova de Origem` — H2 agrupador de H3 é markdown legítimo. Pegou `autenticacao-de-requisicoes-por-assinatura-digital.md`, 1.552 palavras e 15 headings. Fix: seção só é vazia se não tem texto **nem subseção de nível maior**. Achado por rodar o gate contra os 1.039 artigos reais antes de entregar, não pela suíte. |
| 2026-07-31 | Detector de placeholder dava falso positivo em notação matemática e não via o frontmatter | ✓ RESOLVIDO | A regex `<...>` com espaço pegava `< 3 log n para n >` e uma tabela com `< nó atual \| ...`; o review do PR #46 apontou ainda `<T extends Base>` (generics, e `typescript` é topic default) e que a varredura recebia só o corpo, deixando passar `title: <título em português>`. Fix: abandonar heurística de formato e comparar contra os **placeholders reais do template** — candidato só é marcado se seus tokens forem subconjunto dos de algum marcador do molde; varredura passou a cobrir o markdown inteiro. |
| 2026-07-31 | Canário do 1-bit em AMD contradisse a pesquisa de código-fonte | ✓ REGISTRADO | A pesquisa confirmou que existe `dequant_q1_0.comp` no backend Vulkan (merge b8742) e que o Ollama 0.30.10 usa b9672, concluindo que a rota era viável. O canário real deu `96%/4% CPU/GPU` a 4,71 tok/s, contra 17,6 tok/s no Metal local. **Suporte declarado no shader ≠ caminho de execução acelerado.** Decisão: manter o bonsai local. Registrado como correção no topo de `PESQUISA-1BIT-AMD.md`. |
| 2026-08-01 | Emiti `CONTRACT_VALID` da feature 023 pulando o `eval-design` | ✓ RESOLVIDO | O `spec-pipeline` dispara `eval-design` quando o PLAN marca output não-determinístico, e a feature é um classificador NLI — o gatilho mais claro possível. Passei direto de `spec-analyze` para o CONTRACT. Corrigido no mesmo ciclo: `EVALS.md` redigido e executado, `.state` com `EVAL_DESIGN_PARTIAL`. O passo pulado era justamente o que exige holdout, a limitação que eu já havia registrado cinco vezes na sessão. |
| 2026-08-01 | **Caso de eval delegado ao Codex esperava o veredito oposto ao que o próprio documento defendia** | ✓ RESOLVIDO | `E-EXT-001` ("merge sort tem O(n²)" contra contexto que não fala de complexidade) esperava `contradita`, citando entailment 0,357 / contradição 0,615 do protótipo. A frase seguinte do mesmo documento dizia que "para ancoragem, a correção deve derivar do contexto, não da memória do modelo" — o que exige `sem apoio`. Na execução: 0,004 / 0,007, neutro dominante, `sem apoio`. Os números citados também não reproduzem (vieram de outra premissa). **A leitura do artefato não pegou; a execução pegou.** Review zero-trust de documento tem de incluir rodar o que ele afirma ser executável. |
